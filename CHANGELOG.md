# Changelog

本插件变更记录遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)
规范，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- `line_order` 支持任意位置的一个或多个 `null` 占位（字符串大小写不敏感或列表中的 JSON `null`）：
  保留顺序位置并将该位置的文字比对判定为通过，包括 OCR 未识别出文字的情况；
  仍执行线标总数量检查，支持单候选、多候选及 `front` / `back` / `all` 规则。

### 变更

- 在插件测试目录维护日志隔离与日志采集 fixture，支持独立运行测试，不再依赖框架根目录的 `conftest.py`。
- 将判定日志整理为独立方法，分离日志摘要与响应组装，保持判定和日志内容不变。
- 请求参数日志保留线序和引导框摘要；每次判定记录候选、排序规则、数量、
  期望文字、识别文字、错误位置和 `null` 跳过位置，区分识别分数与检测分数。
  主动跳过的位置未识别出文字时不再单独报警。
- 线标 RF-DETR 分割模型升级为 `rfdetr-seg-nano_v1.3.onnx`，改善 T1 型号
  紧密排列线标的漏检和多检问题。

## [1.2.1] - 2026-08-06

### 修复

- 线标 RF-DETR 分割模型升级为 `rfdetr-seg-nano_v1.2.onnx`，修复紧密排列
  线标在分割结果中相互粘连的问题。
- 线标 RF-DETR mask 概率阈值调整为 `0.7`，并支持通过
  `PANEL_LABEL_MASK_THRESHOLD` 在启动时覆盖和校验。
- 线标实例去重增加实际 mask 多边形重叠校验，避免相邻线标的旋转外接框高度
  重叠时将两个真实实例误判为重复检测。

## [1.2.0] - 2026-07-31

### 新增

- `line_order` 支持使用分号下发多组候选线标顺序，识别结果完整匹配任一候选即
  判定通过；原有单候选字符串和一维列表调用保持兼容。

### 变更

- OCR 中间结果新增框架统一的 `OCRToken` 列表，分别保留文字区域、识别置信度和
  检测置信度；请求参数复用公共视觉参考结构。
- 场景配置迁移到框架 `SceneSettings`，移除重复环境变量解析；非法布尔值或阈值
  将在启动阶段明确报错。

## [1.1.2] - 2026-07-28

### 变更

- 将多边形包含、重叠和实例去重拆为独立几何模块，旧 `utils` 导入路径保持兼容；
  同时简化引导框过滤、OCR 仲裁和自动标注测试夹具。
- ONNX 方向分类与文字识别的 `inference.yml` 路径改为显式场景配置，确保离线
  发布权重收集会包含运行时必需的预处理和字典 metadata。
- 方向分类与 CTC 识别改为消费框架类型化结果；OCR pipeline 显式接收模型 metadata
  路径，模型对象不再持有或接收仅供 runner 加载的 ONNX 路径。
- 场景注册切换至框架 `ScenarioRegistry`，保持检测、方向仲裁、文字识别和 API
  响应行为不变。
- 检测、方向分类和文字识别 ONNX runner 改为由场景业务层统一创建并注入；
  三个子模型支持随服务关闭统一释放，初始化失败会回滚已创建资源。
- 面板标签分割检测默认模型由 YOLO `best.onnx` 切换至
  `rfdetr-seg-nano.onnx`；OCR、去重、排序和 API 响应契约保持不变。
- 新增默认开启的 `PANEL_LABEL_CPU_FAST_PATH`：RF-DETR 仅解码检测框局部 mask，
  ROI 展平仅在 polygon 紧致区域内分配和计算，保持文本、类别、顺序、置信度和
  位置反映射契约。
- `PANEL_LABEL_CPU_FAST_PATH=false` 可同时恢复完整 mask 和旧 ROI 展平路径；
  单候选局部处理失败时自动回退旧路径并限频记录 warning。

## [1.1.1] - 2026-07-14

### 修复

- 修复 XB3 两排线标使用默认线性排序导致识别顺序错误的问题。

### 变更

- 线标 OCR 的 ONNX CTC 识别改为按请求批量执行，避免逐个 ROI 调用推理后端。
- 线标 OCR 的 ONNX CTC 识别使用顺序执行模式，降低单路图调度开销。

## [1.1.0] - 2026-07-13

### 新增

- 新增 PP-LCNet 文本行方向分类与 PP-OCRv5 文字识别 ONNX 适配器。
- 新增 Paddle GPU 与 ONNX Runtime CUDA 的真实样本一致性测试。

### 变更

- 删除失效的文本检测配置、数据字段和裁图管线，将 ROI、文字与回流裁图
  收敛为 1:1 列表流。
- 方向分类和文字识别统一切换至 ONNX Runtime，移除插件运行时的
  PaddleOCR/PaddleX 推理依赖。
- 模型配置改为直接引用 `weights/panel_label/v2` 下的 ONNX 文件。
- 保持现有 API、返回字段、阈值、排序和低置信方向双向仲裁行为不变。
